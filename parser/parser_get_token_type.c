#include "pareser_private.h"
#include "token_defines.h"

int			parser_get_token_type(t_parser *parser, int token_type)
{
	// init
	if (pareser->state == INIT)
	{
		if (token_type == TOKEN_CHNAME || token_type == TOKEN_CHCOM)
		esle if (token_type == TOKEN_LABEL_WORD)
		else if (token_type == TOKEN_LFEED)
	}
}